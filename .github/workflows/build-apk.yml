name: Build APK
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
      - name: Make gradlew executable
        run: chmod +x VideoDownloader/gradlew
      - name: Build Debug APK
        run: VideoDownloader/gradlew assembleDebug --stacktrace -p VideoDownloader
      - name: Upload APK
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: VideoDownloader-APK
          path: VideoDownloader/app/build/outputs/apk/debug/*.apk
          retention-days: 30
